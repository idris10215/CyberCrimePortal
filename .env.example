# Keep this file local. Never expose this value to the frontend.
OPENAI_API_KEY=replace_with_your_api_key

# Optional. This model must support Structured Outputs with the Responses API.
OPENAI_MODEL=gpt-5-mini

# Local demo authentication only. Replace for local runs; do not commit a real secret.
JWT_SECRET=replace-with-local-demo-secret
